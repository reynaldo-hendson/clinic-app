import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        display: "flex",
        width: "100%",
        flexDirection: "column",
        fontSize: 15,
        color: "rgba(0, 0, 0, 1)",
        lineHeight: 16.5,
        height: 120,
    },
});
